# SKU 匹配算法说明（与代码一致）

## 万邑通SKU 列

同一张 SKU 表中的「万邑通SKU」列用于**全表列举**所有真实 SKU（无行与行对应关系）。
程序会先扫描整列，去重后形成**唯一 SKU 池**，再对每一行 `custom Label` 做匹配。

## 三类情况（统一为候选生成）

1. **含店铺前缀**：在去掉数量、批次后缀后，按当前行 `店铺账号` 对应的简称（可多简称）尝试去掉前缀，得到候选。
2. **不含店铺前缀**：不去简称前缀，仅对清洗后的串做池匹配。
3. **LZ- 双义**：真实 SKU 与 Label 都可能含 `LZ-`。在清洗后的串上同时保留「整段」与「去掉首段 LZ-」两种候选。

## 清洗顺序（与文档一致）

1. 分隔符与常见脏前缀归一（空格、`_`、`—`、重复简称、`aXX-` 等保守规则）
2. 从原始 `custom Label` 解析数量：`*n`、`(n)`、`（n）` → 写入「数量」（多数字冲突时默认 1）
3. 去掉数量标记后再去掉末尾批次后缀：`-A/-B/_C` 等（可循环去多次）

## 匹配规则

1. **精确匹配**：任一候选经规范化后与池中某 SKU 完全一致 → 写入「匹配SKU」，状态成功。
2. **多精确命中**：多个不同池 SKU 同时命中 → 失败（歧义），「匹配SKU」留空。
3. **近似匹配**：无精确命中时，用 `difflib` 序列比对，在池中找**唯一**高分命中（默认比例 ≥0.88 且与第二名有足够分差）→ 写入「近似匹配」，状态成功；「匹配SKU」留空。
4. 近似也不唯一或不够分 → 失败，原因见「失败原因」与「匹配审计」。

## 结果表新增列

- **近似匹配**：仅近似成功时有值，否则为空。
